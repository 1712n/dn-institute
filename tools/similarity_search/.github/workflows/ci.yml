name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Node.js
      node-version: '14'
    - run: npm install
    - run: npm test
    - run: npm run lint