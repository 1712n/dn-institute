{
  "name": "qa-bot-worker",
  "version": "1.0.0",
  "description": "Article QA bot for dn-institute — Cloudflare Worker",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts",
    "test": "vitest"
  },
  "dependencies": {
    "hono": "^4.3.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.2.6",
    "@cloudflare/workers-types": "^4.20240403.0",
    "vitest": "1.3.0",
    "wrangler": "^3.47.0"
  }
}
