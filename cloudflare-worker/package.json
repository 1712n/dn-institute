{
  "name": "article-checker-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for QA checks on article submissions",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "@octokit/rest": "^20.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.78.0",
    "vitest": "^2.0.0"
  }
}
