    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "test:integration": "vitest --environment=workerd",
    "test:watch": "vitest --watch"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",