{
  "name": "qa-bot-worker",
  "version": "1.0.0",
  "description": "🌰 Cloudflare Worker for automated PR quality checks",
  "main": "index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest"
  },
  "dependencies": {
    "hono": "^3.12.0",
    "@octokit/webhooks-methods": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "wrangler": "^3.22.0"
  }
}